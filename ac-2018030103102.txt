Problem1000:
#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
int main()
{
	int a, b;
	while (scanf("%d%d", &a, &b) != EOF)
	printf("%d\n", a + b);
	return 0;
}
------------------------------------------------------
Problem1001:
#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
int main()
{
	int a, b;
	int n,i;
	scanf("%d", &n);
	for (i = 0; i < n; i++)
	{
		scanf("%d%d", &a, &b);
		printf("%d\n", a + b);
	}
	return 0;
}
------------------------------------------------------
Problem1002:
#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
int main()
{
	int a, b;
	while (scanf("%d%d", &a, &b), a != 0 || b != 0)
		printf("%d\n", a + b);
	return 0;
}
------------------------------------------------------
Problem1003:
#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
int main()
{
	int a, b = 0;
	int n,i;
	while (scanf("%d", &n) ,n!= 0)
	{
		for (i = 0; i < n; i++)
		{
			scanf("%d", &a);
			b += a;
		}
		printf("%d\n", b);
		b = 0;
	}
	return 0;
}
------------------------------------------------------
Problem1004:
#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
int main()
{
	int a, b = 0;
	int n, i, j;
	int loop;
	scanf("%d", &loop);
	for (j = 0; j < loop; j++)
	{
		scanf("%d", &n);
		for (i = 0; i < n; i++)
		{
			scanf("%d", &a);
			b += a;
		}
		printf("%d\n", b);
		b = 0;
	}
	return 0;
}
------------------------------------------------------
Problem1005:
#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
int main()
{
	int a, b = 0;
	int n, i, j;
	//int loop;
	//scanf("%d", &loop);
	//for (j = 0; j < loop; j++)
	//{
	while (scanf("%d", &n)!=EOF)
	{
		for (i = 0; i < n; i++)
		{
			scanf("%d", &a);
			b += a;
		}
		printf("%d\n", b);
		b = 0; 
	}
	//}
	return 0;
}
------------------------------------------------------
Problem1006:
#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
int main()
{
	int	a, b;
	while (scanf("%d%d", &a, &b) != EOF)
	{
		printf("%d\n\n", a + b);
	}
	return 0;
}
------------------------------------------------------
Problem1007:
#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
int main()
{
	int	a, b = 0;
	int n, i, j, loop;
	scanf("%d", &loop);
	for (i = 0; i < loop; i++)
	{
		scanf("%d", &n);
		for (j = 0; j < n; j++)
		{
			scanf("%d",&a);
			b += a;
		}
		printf("%d\n\n", b);
		b = 0;
	}
	return 0;
}
------------------------------------------------------
Problem1008:
#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
int main()
{
	int n;
	char ascii[1005];
	int i, j;
	scanf("%d", &n);
	if (n >= 0 && n <= 1000)
	{
		for (i = 0; i < n; i++)
		{
			scanf("%d", &ascii[i]);
			if (ascii<32)
			{
				return 0;
			}
		}
		printf("%s", ascii);
	}
	return 0;
}
------------------------------------------------------
Problem1009:
#define _CRT_SECURE_NO_WARNINGS
#include<iostream>
#include<cstdio>
using namespace std;
int main()
{
	double wage[13], sum = 0;
	int i;
	for ( i = 0; i < 12; i++)
	{
		cin >> wage[i];
		sum += wage[i];
	}
	printf("￥%.2lf\n", sum / 12);
	return 0;
}
------------------------------------------------------
Problem1010:
#define _CRT_SECURE_NO_WARNINGS
#include <iostream>
#include <iomanip>
#include <cstring>
#include <cstdio>
using namespace std;

int main()
{
	char s[1000004];
	while (gets(s))
	{
		int l = strlen(s);
		int flag = 0;
		for (int i = 0; i < l; ++i)
		{
			if (s[i] != 'A' && s[i] != 'B' && s[i] != 'C' && s[i] != 'D' && s[i] != 'F' &&s[i] != ' ')
			{
				flag = 1;
				cout << "Unknown" << endl;
				break;
			}
		}
		int sum = 0;
		int length = 0;
		if (flag == 0)
		{
			for (int j = 0; j < l; ++j)
			{
				if (s[j] == 'A')
				{
					sum += 4;
					length++;
				}
				if (s[j] == 'B')
				{
					sum += 3;
					length++;
				}
				if (s[j] == 'C')
				{
					sum += 2;
					length++;
				}
				if (s[j] == 'D')
				{
					sum += 1;
					length++;
				}
				if (s[j] == 'F')
				{
					sum += 0;
					length++;
				}
			}

			double average = sum * 1.0 / (length *1.0);
			cout << fixed << setprecision(2) << average << endl;

		}
	}
	return 0;
}

------------------------------------------------------
Problem1011:
#define _CRT_SECURE_NO_WARNINGS
#include<stdio.h>
int main()
{
	int a[2] = { 0 }, b[2] = { 0 }, c[2] = { 0 };
	int n;
	int i, j;
	scanf("%d", &n);
	for ( i = 0; i < n; i++)
	{
		for ( j = 0; j < 2; j++)
		{
			scanf("%d%d%d", &a[j], &b[j], &c[j]);
		}
		if (a[0] > a[1])
		{
			printf("First\n");
		}
		else if (a[0] < a[1]) 
		{
			printf("Second\n");
		}
		else if (a[0] == a[1])
		{
			if (b[0] > b[1])
			{
				printf("First\n");
			}
			else if (b[0] < b[1])
			{
				printf("Second\n");
			}
			else if (b[0] == b[1])
			{
				if (c[0] > c[1])
				{
					printf("First\n");
				}
				else if (c[0] < c[1])
				{
					printf("Second\n");
				}
				else if (c[0] == c[1])
				{
					printf("Same\n");
				}
			}
		}
	}
	return 0;
}
------------------------------------------------------
Problem1012:
#define _CRT_SECURE_NO_WARNINGS
#include<stdio.h>
int main()
{
	int i, j;
	double usd, max, rate[13] = { 0 };
	scanf("%lf", &usd);
	for ( i = 0; i < 12; i++)
		scanf("%lf", &rate[i]);
	max = rate[0];
	for (j = 0; j < 12; j++)
	{
		if (rate[j]>max)
			max = rate[j];
	}
	printf("%.2lf\n", max*usd);
	return 0;
}
------------------------------------------------------
Problem22124:
#include<stdio.h>
int main()
{
	double a, b, h;
	double area;
	scanf("%lf%lf%lf", &a, &b, &h);
	if (a>0 && b>0 && h>0)
	{
		area = (1.0)*(((a + b)*h) / 2);
		printf("%.2lf\n", area);
	}
	else
		return 0;
}
------------------------------------------------------
Problem22125:
#include<stdio.h>
int main()
{
	int head, foot;
	int x, y;
	scanf("%d%d", &head, &foot);
	x = (4 * head - foot) / 2;
	y = (foot - 2 * head) / 2;
	printf("%d %d\n", x, y);
	return 0;
}

------------------------------------------------------
Problem22126:
#include<stdio.h>
int main()
{
	long a, b;
	long temp;
	scanf("%ld%ld", &a, &b);
	temp = a;
	a = b;
	b = temp;
	printf("%ld %ld", a, b);
	return 0;
}

------------------------------------------------------
Problem22127:
#include<stdio.h>
int main()
{
	int n;
	scanf("%d", &n);
	int a, b, c, d;
	d = n / 1000;
	c = n / 100 % 10;
	b= n % 100 / 10;
	a = n % 10;
	printf("%d%d%d%d", a,b,c,d);
	return 0;
}

------------------------------------------------------
Problem22128:
#include<stdio.h>
#include<math.h>
int main()
{
	int a, b, c;
	double res1, res2;
	scanf("%d%d%d", &a, &b, &c);
	res1 = ((-b) + sqrt(pow(b, 2) - (4 * a*c))) / ((2)*a);
	res2 = ((-b) - sqrt(pow(b, 2) - (4 * a*c))) / ((2)*a);
	if (sqrt(pow(b, 2) - (4 * a*c))==0)
		printf("%.3lf\n", res1);
	else if (sqrt(pow(b, 2) - (4 * a*c)) > 0)
		printf("%.3lf %.3lf\n", res1, res2);
	else
		printf("NOANSWER\n");
	return 0;
}

------------------------------------------------------
Problem22129:
#include<stdio.h>
int main()
{
	double s[50];
	double t;
	int i;

	for (int j = 0; j < 3; j++)
	{
		scanf("%lf", &s[j]);
	}
	for (i = 0; i < 3; i++)
	{
		if (s[i-1] > s[i])
		{
			t = s[i-1];
			s[i-1] = s[i];
			s[i] = t;
		}
	}
	for (int j = 0; j < 3; j++)
	{
		printf("%.3lf ", s[j]);
	}
	printf("\n");
}

------------------------------------------------------
Problem22130:
#include<stdio.h>
#include<math.h>
int main()
{
	int a, b, c;
	double s;
	scanf("%d%d%d", &a, &b, &c);
	if ((a + b) > c && ((a + c) > b) && ((b + c) > a))
	{
		s = (0.25)*sqrt((a + b + c)*(a + b - c)*(a + c - b)*(b + c - a));
		printf("%.2lf\n", s);
	}
	else
	{
		printf("Can't\n");
	}
	return 0;
}
------------------------------------------------------
Problem22131:
#include<stdio.h>
int main()
{
	long n[3];
	int i, j, k;
	long t;
	long max, min;
	for (i = 0; i < 3; i++)
	{
		scanf("%ld", &n[i]);
	}
	max = min = n[0];
	for (j = 0; j < 3; j++)
	{
		if (n[j] > max)
			max = n[j];
	}
	for (k = 0; k < 3; k++)
	{
		if (n[k] < min)
			min = n[k];
	}
	printf("%ld %ld\n", max, min);
	return 0;
}
------------------------------------------------------
Problem22133:
#include<stdio.h>
int main()
{
	long long n = 0, sum = 0;
	long long i;
	scanf("%lld", &n);
	for ( i = 1; i <= n; i++)
	{
		sum += i;
	}
	printf("%lld\n", sum);
}
------------------------------------------------------
Problem22134:
#include<stdio.h>
int main()
{
	int n = 0,i;
	long sum = 1;
	scanf("%d", &n);
	for (i = 1; i <= n; i++)
	{
		sum *= i;
	}
	printf("%ld\n", sum);
}

------------------------------------------------------
Problem22136:
#include<stdio.h>
int main()
{
	long  n, i;
	long long sum = 0;
	scanf("%ld", &n);
	for (i = 1; i <= n; i++)
	{
		sum += i*i;
	}
	printf("%lld\n", sum);
}
------------------------------------------------------
Problem22137:
#include<stdio.h>
int main()
{
	int n,m[1000];
	int i, j, k;
	int max, min;
	scanf("%d", &n);
	for ( i = 0; i < n; i++)
	{
		scanf("%d", &m[i]);
	}
	max = min = m[0];
	for (j = 0; j < n; j++)
	{
		if (m[j] > max)
			max = m[j];
	}
	for (k = 0; k < n; k++)
	{
		if (m[k] < min)
			min = m[k];
	}
	printf("%d %d\n", max, min);
}
------------------------------------------------------
Problem22138:
#include<stdio.h>
int main()
{
	long n;
	long i,j;
	long subs=1;
	long s=0;

	scanf("%ld", &n);
	if (n <= 20)
	{
		for (i = 1; i <= n; i++)
		{
			for (j = 1; j <= i; j++)
			{
				subs *= j;
			}
			s += subs;
			subs = 1;
		}
	}
	printf("%ld\n", s);
}
------------------------------------------------------
Problem22139:
#include<stdio.h>
int main()
{
	double n;
	double sum=0;
	double pos=0, neg=0;
	double c = 0;
	while (scanf("%lf", &n) , n != 0.0)
	{
		c++;
		if (c>=10000)
		{
			break;
		}
		if (n>0)
		{
			pos++;
		}
		if (n<0)
		{
			neg++;
		}
		sum += n;
	}
	printf("%.0lf %.0lf ", pos, neg);
	printf("%.3lf\n", sum);
}
------------------------------------------------------
Problem22141:
#define _CRT_SECURE_NO_WARNINGS
#include<stdio.h>
int main()
{
	int a[100000];
	int i, j, n;
	int min = 0, max = 0, maxp = 0, minp = 0;
	scanf("%d", &n);
	for ( i = 0; i < n; i++)
	{
		scanf("%d", &a[i]);
	}
	max = a[0], min = a[0];
	for ( j = 0; j < n; j++)
	{
		if (a[j] > max)
		{
			max = a[j];
			maxp = j + 1;
		}
		if (min > a[j])
		{
			min = a[j];
			minp = j + 1;
		}
	}
	printf("%d %d\n", max, maxp);
	printf("%d %d\n", min, minp);
	return 0;
}
------------------------------------------------------
Problem22153:
#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <stdlib.h>
int main()
{
	int s = 1, h;
	int i, j;
	scanf("%d", &h);
	if (h >= 0 && h <= 20)
	{
		printf("1\n");
		for (i = 2; i <= h; s = 1, i++)
		{
			printf("1 ");
			for (j = 1; j <= i - 2; j++)
				printf("%d ", (s = (i - j) * s / j));
			printf("1\n");
		}
	}
	else
	{
		;
	}
	return 0;
}
------------------------------------------------------
Problem22154:
#include<iostream>
#include<cstdio>
#include<cstring>
using namespace std;
int main()
{
	char s[100];
	int i, j, b, c = 0;
	cin >> s;
	int l = strlen(s);
	for ( j = 0; s[j]!='.'&&s[j]!='\0'; j++)
	{
		;
	}
	for (i = 0, b = j - 1; i < j; i++, b--)
	{
		if (s[i] == s[b])
			c++;
	}
	if (c == i)
	{
		cout << "Yes" << endl;
	}
	else
	{
		cout << "No" << endl;
	}
	return 0;
}
------------------------------------------------------
